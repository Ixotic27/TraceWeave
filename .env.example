# Copy to .env only after a dedicated project is confirmed free and migrated.
# Never commit or paste a real secret key into browser code or chat.
TRACEWEAVE_CLOUD_ENABLED=0
SUPABASE_URL=
SUPABASE_SECRET_KEY=
# Hosted Render mode uses only this publishable/anon key and authenticated users.
SUPABASE_PUBLIC_KEY=
